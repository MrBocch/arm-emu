package vm

import (
	//"fmt"
	//"github.com/MrBocch/arm-emu/cmd/assembler"
)

// hardvard architecture, code and data are separated 
func HardvardRun(t []assembler.Token) {
	/*
	MAX_MEMORY = 3000
	if len(t) >= MAX_MEMORY { panic("OVER CODE MEMORY LIMIT") }
	var r0, r1, r2, r3, r4, r5, r6, r7, r8, r9, r10, r11, r12, lr, sp, pc uint32  
	
	for i, v := range t {
		fmt.Printf("%v -> %v\n", i, v)
	}
	*/
}
